# Node Environment
NODE_ENV=dev

# Slack Configuration
# 테스트 시 더미 값 사용 가능
SLACK_TOKEN=dummy-token-for-test
SLACK_CHANNEL=#test-channel
# 선택사항: 에러 알림 채널 (비워두거나 더미 값 사용 가능)
SLACK_ERROR_CHANNEL=#test-errors

# Database
DB_PATH=./data/articles.db

# Crawler Configuration
DEFAULT_TIMEOUT_MS=30000
